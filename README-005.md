1. What is the output?

    ```python
        >>> a = [[]]*3
        >>> a[1].append(1)
        >>> print(a)
    ```
    ---------------------------------------

2. Write a function to manipulate the given date of birth.
    
    ---------------------------------------